<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- solid 设置stroke设置的边框以内的颜色 -->
    <solid android:color="#f5f5f5"/>
    <!-- stroke主要设置组件的边框。width为边框宽度，color为边框颜色 -->
    <stroke android:width="@dimen/dp_0" android:color="#000000" />
    <!-- corners 设置边框四角弧度 -->
    <corners android:radius="@dimen/dp_30"/>
    <!-- padding主要设置组件里内容距离组件内边框的间距 -->
    <padding android:left="@dimen/dp_10" android:top="@dimen/dp_5"  android:bottom="@dimen/dp_5" android:right="@dimen/dp_10"/>
</shape>